// A function which returns the value of the input field with id: player-screen-name.
function selectScreenName() {
	return document.getElementById("player-screen-name").value;
}

// A function which returns the value of the input field with id: quiz-code-player.
function selectQuizCode() {
	return document.getElementById("quiz-code-player").value;
}
